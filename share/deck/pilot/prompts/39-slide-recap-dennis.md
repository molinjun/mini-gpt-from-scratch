---
slide: 39
title: "Recap: Dennis Through Mini-GPT"
pilot: true
---

Create one 16:9 presentation slide image in a dark technical blueprint style.

Visible text to render exactly:
- Recap: Dennis Through Mini-GPT
- Text -> tokens -> vectors
- Blocks -> logits -> probabilities
- Sample -> append -> repeat
- Denn
- d e n n
- [4, 5, 14, 14]
- logits = raw scores
- softmax = probabilities
- Dennis

Layout:
- Long left-to-right pipeline.
- Start with text "Denn".
- Show character-level tokenizer boxes: d e n n.
- Show small token IDs only, such as [4, 5, 14, 14], with a tiny label "vocab_size = 27".
- Show embedding vectors, Transformer blocks, logits, softmax probabilities, sampled token, and final "Dennis".
- Use arrows and compact labels so the full path is easy to follow.
- Make this a summary flow, not a dense architecture diagram.

Visual style:
- Deep navy blueprint grid background.
- Cyan and electric-blue technical line work.
- Amber highlight only for the sampled token.
- Crisp high-contrast sans-serif text.
- No slide number, no logo, no watermark, no Chinese text.

Avoid:
- Paragraphs.
- Too many numeric values.
- Random tokens that distract from Denn -> Dennis.
- Large token IDs such as 50256 or 100000.
