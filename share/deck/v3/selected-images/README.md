# V3 Selected Images Workspace

Use these folders to collect the best image candidates from v1, v2, v3, or any new source before final deck assembly.

Do not edit PPTX, PDF, or `index.html` while sorting images. Put copied image candidates into the matching module folder, then we will choose the final slide order and notes module by module.

## Folders

- `01-opening-why-mini-gpt/`
  - Cover, agenda, Transformer paper / next-token idea, MicroGPT / Mini-GPT motivation.
- `02-tokenizer-embedding/`
  - Tokenizer, character tokens, Dennis to IDs, token embedding, position embedding, decoder-only map overview if used as the bridge after embeddings.
- `03-neural-network-basics/`
  - Function view, neuron, MLP, ReLU, prediction/loss, backprop, AdamW.
- `04-transformer-architecture/`
  - Transformer block, attention intuition, Q/K/V, scores/softmax, causal mask, multi-head attention, RMSNorm/residuals, MLP block, logits.
- `05-training-inference/`
  - Training Mini-GPT, training loop, loss curve, generation, sampling loop, temperature.
- `06-recap-closing/`
  - Dennis recap, code inspection, references, Q&A / thank you.

## Current V3 Direction

The active V3 story is a 37-slide structure:

1. Why Mini-GPT
2. Tokenizer and Embedding
3. Decoder-only overview
4. Neural Network Basics
5. Transformer Architecture
6. Training and Inference
7. Recap and Closing

