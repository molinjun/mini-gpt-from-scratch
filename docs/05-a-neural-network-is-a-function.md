# 06 A Neural Network Is a Function

## Section

Neural Network Basics

## Slide-Visible Text

- Input `x` maps to output `y`
- Weights choose the line
- Training fits the data

## Speaker Notes

The first mental model is simple: a neural network is a function with parameters. It takes input numbers and produces output numbers. At the beginning, the function is usually wrong. Training changes the parameters so the output fits the data better.

## Visual Direction

Line-fitting example: data points on a small plot, a wrong initial line, and adjustable knobs labeled `W` and `b`.

## Target Image

`share/deck/v2/06-slide-a-neural-network-is-a-function.png`
